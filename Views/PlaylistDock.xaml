<UserControl x:Class="mono.Views.PlaylistDock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="TrackItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="8,0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceAlt}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#ede9fb"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="ListContainer"
            Background="{StaticResource Background}"
            BorderBrush="{StaticResource Border}" BorderThickness="0,1,0,0"
            MaxHeight="220" ClipToBounds="True">
        <ListView x:Name="TrackList"
                  Background="Transparent"
                  BorderThickness="0"
                  Padding="0"
                  Margin="0"
                  SelectionMode="Single"
                  AllowDrop="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ItemContainerStyle="{StaticResource TrackItemStyle}"
                  ItemsSource="{Binding Queue}"
                  SelectionChanged="TrackList_SelectionChanged"
                  PreviewMouseLeftButtonDown="TrackList_PreviewMouseLeftButtonDown"
                  PreviewMouseMove="TrackList_PreviewMouseMove"
                  PreviewMouseLeftButtonUp="TrackList_PreviewMouseLeftButtonUp"
                  DragOver="TrackList_DragOver"
                  DragLeave="TrackList_DragLeave"
                  Drop="TrackList_Drop">
            <ListView.Resources>
                <Style TargetType="ContextMenu">
                    <Setter Property="Background" Value="{StaticResource Surface}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Padding" Value="0,4"/>
                    <Setter Property="FontFamily" Value="{StaticResource FontInter}"/>
                    <Setter Property="FontSize" Value="12"/>
                </Style>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="12,6"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="MenuItem">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Bd" Property="Background"
                                                Value="{StaticResource SurfaceAlt}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="Separator">
                    <Setter Property="Background" Value="{StaticResource Border}"/>
                    <Setter Property="Height" Value="1"/>
                    <Setter Property="Margin" Value="0,2"/>
                </Style>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="IndexText" Grid.Column="0"
                                   Text="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource ItemIndexConverter}}"
                                   Foreground="{StaticResource TextSecondary}" FontSize="11"
                                   FontFamily="Consolas"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TitleText" Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource TextPrimary}" FontSize="12"
                                   TextTrimming="CharacterEllipsis">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}{1}{2}">
                                    <Binding Path="Artist"/>
                                    <Binding Source=" - "/>
                                    <Binding Path="Title"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding Format}"
                                   Foreground="{StaticResource TextSecondary}" FontSize="10"
                                   FontFamily="Consolas"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3"
                                   Text="{Binding Duration, StringFormat='{}{0:m\\:ss}'}"
                                   Foreground="{StaticResource TextSecondary}" FontSize="11"
                                   FontFamily="Consolas"
                                   VerticalAlignment="Center" HorizontalAlignment="Right"
                                   Margin="0,0,4,0"/>
                    </Grid>

                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                            <Setter TargetName="IndexText" Property="Text" Value="▶"/>
                            <Setter TargetName="IndexText" Property="Foreground" Value="{StaticResource Accent}"/>
                            <Setter TargetName="TitleText" Property="Foreground" Value="{StaticResource Accent}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</UserControl>
